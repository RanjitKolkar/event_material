# NEVER commit real values. Use Streamlit Cloud Secrets or environment variables.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_PASSWORD
APP_ENCRYPTION_KEY=REPLACE_WITH_FERNET_KEY
# Optional: persistent storage location. Keep outside the Git repository.
EVENT_DATA_DIR=/persistent/event_material_generator
