# Copy the values below into Streamlit Cloud -> App settings -> Secrets.
# NEVER commit a real secrets.toml file.
ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "replace-with-a-unique-long-password"
APP_ENCRYPTION_KEY = "replace-with-a-fernet-key"
# Optional persistent directory if your deployment provides one.
# EVENT_DATA_DIR = "/persistent/event_material_generator"
